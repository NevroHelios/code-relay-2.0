/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'cdn.noitatnemucod.net',
      'shorturl.at'
    ],
  },
  // ...existing config options...
};

module.exports = nextConfig;
